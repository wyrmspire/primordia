#!/usr/bin/env bash
set -euo pipefail
echo "[stop] compose down"
docker compose down --remove-orphans || true
echo "[stop] firestore container"
docker rm -f primordia-firestore primordia-firestore-1 || true
echo "í»‘ stopped"
