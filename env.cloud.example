# -----------------------------------------------------------------------------
# EXAMPLE ENVIRONMENT FOR CLOUD DEPLOYMENT
#
# 1. Copy this file to a new file named .env (e.g., `cp env.cloud.example .env`)
# 2. Fill in the values for your specific Google Cloud project.
# 3. Use this .env file when running `./deploy.sh`
# -----------------------------------------------------------------------------

# --- Required ---
# Your Google Cloud Project ID.
PROJECT_ID="your-gcp-project-id"

# The GCS bucket where the bridge will store scaffolded code.
# This MUST be a globally unique name.
WORKSPACE_BUCKET="a-globally-unique-bucket-name"

# The GCP region for deployments.
REGION="us-central1"

# --- Optional (Defaults are recommended) ---
# Firestore collection names
CACHE_COLLECTION="primordia_cache"
TASKS_COLLECTION="primordia_jobs"

# Pub/Sub topic and subscription names for the cloud
PUBSUB_TOPIC="primordia-builds"
PUBSUB_SUB="primordia-worker-sub"
