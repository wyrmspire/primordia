# -----------------------------------------------------------------------------
# EXAMPLE ENVIRONMENT FOR LOCAL DOCKER COMPOSE
#
# 1. Copy this file to a new file named .env (e.g., `cp env.local.example .env`)
# 2. These values are pre-configured for the local emulator stack and rarely need changing.
# -----------------------------------------------------------------------------

# --- Core Project Settings (used by emulators) ---
PROJECT_ID="ticktalk-472521"
REGION="us-central1"
WORKSPACE_BUCKET="primordia-bucket"

# --- Firestore Collections ---
CACHE_COLLECTION="primordia_cache"
TASKS_COLLECTION="primordia_jobs"

# --- Pub/Sub Configuration ---
PUBSUB_TOPIC="primordia-builds"
PUBSUB_SUB="primordia-worker-sub"

# --- Local Proxy Allowlist ---
# Comma-separated list of internal Docker URLs the proxy is allowed to call.
PROXY_ALLOWLIST='http://primordia-local-service-hot-swap-final:8080'
